# Alexa LLM Chat - Environment Variables Example
# Copy this file to .env and fill in your actual values

# =============================================================================
# REQUIRED ENVIRONMENT VARIABLES
# =============================================================================

# OpenRouter API Configuration (REQUIRED)
# Get your API key from https://openrouter.ai
OPENROUTER_API_KEY=your_openrouter_api_key_here

# AWS DynamoDB Table Names (REQUIRED - set automatically by SAM deployment)
CONVERSATION_CONTEXT_TABLE=alexa-llm-chat-conversation-context-dev
SUBSCRIPTION_STATUS_TABLE=alexa-llm-chat-subscription-status-dev
ANALYTICS_TABLE=alexa-llm-chat-analytics-dev

# =============================================================================
# OPTIONAL ENVIRONMENT VARIABLES (with defaults)
# =============================================================================

# OpenRouter API Configuration
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# User Limits and Models
FREE_USER_DAILY_LIMIT=5
FREE_USER_MODEL=openai/gpt-3.5-turbo
PREMIUM_USER_MODEL=openai/gpt-4

# Performance Configuration
MAX_CONTEXT_TOKENS=4000
RESPONSE_TIMEOUT_MS=7000

# Environment Configuration
ENVIRONMENT=dev

# =============================================================================
# ALEXA SKILL CONFIGURATION (set after skill deployment)
# =============================================================================

# Alexa Skill ID (set after creating the skill)
ALEXA_SKILL_ID=amzn1.ask.skill.your-skill-id-here

# Alexa Stage (development or live)
ALEXA_STAGE=development

# In-Skill Purchase Product IDs (set after creating ISP products)
PREMIUM_MONTHLY_PRODUCT_ID=premium_monthly

# =============================================================================
# AWS CONFIGURATION (usually set by AWS Lambda environment)
# =============================================================================

# AWS Region (usually set automatically)
AWS_REGION=us-east-1

# AWS Lambda Function Configuration (set automatically)
AWS_LAMBDA_FUNCTION_NAME=alexa-llm-chat-dev
AWS_LAMBDA_FUNCTION_VERSION=$LATEST

# =============================================================================
# DEVELOPMENT/TESTING CONFIGURATION
# =============================================================================

# Node Environment
NODE_ENV=development

# Logging Level (error, warn, info, debug)
LOG_LEVEL=info

# Enable/Disable Features for Testing
ENABLE_ANALYTICS=true
ENABLE_ISP=true
ENABLE_CONTEXT_TRUNCATION=true

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 
# 1. NEVER commit your actual .env file to version control
# 2. Keep your OpenRouter API key secure and rotate it regularly
# 3. Use AWS IAM roles and policies for production deployments
# 4. Consider using AWS Systems Manager Parameter Store for sensitive values
# 5. The .env file is only used for local development - AWS Lambda uses environment variables set in template.yaml
#
# =============================================================================